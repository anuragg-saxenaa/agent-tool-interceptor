{
  "name": "agent-tool-interceptor",
  "version": "1.0.0",
  "description": "Runtime CLI layer that intercepts AI agent tool calls and enforces execution boundaries",
  "main": "dist/index.js",
  "bin": {
    "agent-tool-interceptor": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts"
  },
  "keywords": ["ai", "agent", "security", "interceptor", "cli"],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "eslint": "^8.55.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "yaml": "^2.3.4"
  }
}
